source 'http://rubygems.org' 

gemspec 

# This Gemfile is only used for the development of this gem.
# It, and Gemfile.lock, are excluded from the built gem.
# The entries in this Gemfile should be kept in-synch with the 
# message-bus.gemfile, which contains these corresponding entries:
#   s.add_runtime_dependency     'bunny', '0.7.8' 
#   s.add_development_dependency 'rspec', '2.8.0'

# Required in production:
  # gem 'bunny',   '0.7.8'
  # 
  # group :development, :test do 
  #   gem 'activerecord', '3.1.3'
  #   gem 'sqlite3',      '1.3.5'
  #   gem 'rspec',        '2.8.0'
  # end
